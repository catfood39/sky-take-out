<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">

    <insert id="insertBatch">
        insert into setmeal_dish (setmeal_id, dish_id, name, price, copies)
        values
            <foreach collection="setmealDishes" item="item" separator=",">
                (#{item.setmealId}, #{item.dishId}, #{item.name}, #{item.price}, #{item.copies})
            </foreach>
    </insert>

    <delete id="deleteBySetmealId">
        delete from setmeal_dish where setmeal_id = #{id}
    </delete>

    <select id="getDishItemBySetmealId" resultType="com.sky.vo.DishItemVO">
        select a.name, a.copies, b.image, b.description
        from setmeal_dish a, dish b
        where a.dish_id = b.id and setmeal_id = #{setmealId}
    </select>

    <select id="getBySetmealId" resultType="com.sky.entity.SetmealDish">
        select * from setmeal_dish where setmeal_id = #{setmealId}
    </select>

    <update id="updateName">
        update setmeal_dish set name = #{name} where dish_id = #{id}
    </update>
</mapper>